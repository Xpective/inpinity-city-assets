# GitHub Push Commands

```bash
git clone https://github.com/Xpective/inpinity-city-assets.git
cd inpinity-city-assets

# kopiere dann den Inhalt dieses Starter-Pakets in das Repo

# optional
python3 -m http.server 8787 -d public

# commit + push
git add .
git commit -m "seed city assets repo with personal buildings starter"
git push origin main
```
